CREATE TABLE student (
id INT AUTO_INCREMENT PRIMARY KEY,
student_name VARCHAR (50) NOT NULL
);

CREATE TABLE studentinfo (
student_id INT,
subject_id INT,
grade INT,
FOREING KEY (student_id) REFERENCES student (id),
FOREING KEY (subject_id) REFERENCES subject (id) );